import React from 'react';

function Count({ style }) {
   return (
      <div className="bg-red">
         <input type="text" />
         <button>+</button>
         <button>-</button>
      </div>
   );
}

export default Count;
